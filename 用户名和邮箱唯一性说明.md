# 用户名和邮箱唯一性功能文档

## 📋 概述

本文档说明了校园动物档案馆系统中的用户名和邮箱唯一性验证功能。

---

## ✅ 邮箱唯一性（自动实现）

### 功能状态：**已启用** ✅

**实现方式**：
- Supabase Auth 自动保证邮箱唯一性
- 无需额外代码或配置

**行为**：
- ✅ 每个邮箱只能注册一次
- ✅ 重复注册时自动返回错误
- ✅ 错误信息：`User already registered`

**测试**：
```bash
# 第一次注册 - 成功
email: test@example.com
password: 123456
username: 测试用户

# 第二次注册相同邮箱 - 失败
email: test@example.com  # ❌ 会提示已注册
password: abcdef
username: 另一个名字
```

---

## 🔐 用户名唯一性（已实现）

### 功能状态：**需要配置数据库函数** ⚙️

### 1️⃣ 用户名验证规则

已在前端实现以下验证：

| 验证项 | 规则 | 错误提示 |
|--------|------|----------|
| 长度 | 2-20 个字符 | "用户名长度必须在 2-20 个字符之间" |
| 字符 | 仅允许中文、字母、数字、下划线 | "用户名只能包含中文、字母、数字和下划线" |
| 唯一性 | 不能与已有用户名重复（不区分大小写） | "该用户名已被占用，请换一个" |
| 必填 | 不能为空 | "用户名不能为空" |

### 2️⃣ 验证时机

- ✅ **注册时**：提交注册表单时验证
- ✅ **修改资料时**：保存新用户名时验证

### 3️⃣ 合法用户名示例

✅ **允许的用户名**：
- `小橘猫` （中文）
- `CampusZoo` （英文）
- `用户123` （中文+数字）
- `user_name` （下划线）
- `萌宠爱好者_2024` （混合）

❌ **不允许的用户名**：
- `a` （太短，少于2个字符）
- `这是一个超级超级超级长的用户名` （太长，超过20个字符）
- `user@123` （包含特殊字符 @）
- `用户名！` （包含特殊字符 ！）
- `user name` （包含空格）

---

## 🛠️ 数据库配置步骤

### 步骤 1：登录 Supabase

1. 访问 https://supabase.com/dashboard
2. 登录您的账号
3. 选择项目：`qbglfhdvkrhxspwrbsgq`

### 步骤 2：创建检查函数

1. 左侧菜单点击 **SQL Editor**
2. 点击 **New Query**
3. 复制粘贴以下 SQL 代码：

```sql
-- 用户名唯一性检查函数
CREATE OR REPLACE FUNCTION check_username_exists(username_to_check TEXT)
RETURNS BOOLEAN
LANGUAGE plpgsql
SECURITY DEFINER
AS $$
DECLARE
  user_count INTEGER;
BEGIN
  -- 查询有多少用户使用了这个用户名（不区分大小写）
  SELECT COUNT(*)
  INTO user_count
  FROM auth.users
  WHERE LOWER(raw_user_meta_data->>'username') = LOWER(username_to_check)
    OR LOWER(raw_user_meta_data->>'username_lower') = LOWER(username_to_check);
  
  -- 如果找到任何用户，返回 true（用户名已存在）
  RETURN user_count > 0;
END;
$$;

-- 添加函数说明
COMMENT ON FUNCTION check_username_exists(TEXT) IS '检查用户名是否已被占用（不区分大小写）';
```

4. 点击 **Run** 按钮执行

### 步骤 3：测试函数

在 SQL Editor 中执行：

```sql
-- 测试 1: 检查一个不存在的用户名（应该返回 false）
SELECT check_username_exists('test_user_12345');

-- 测试 2: 查看所有用户名
SELECT 
  email,
  raw_user_meta_data->>'username' as username
FROM auth.users;
```

### 步骤 4：验证功能

配置完成后，尝试：

1. 注册一个新用户（例如用户名：`测试用户A`）
2. 退出登录
3. 尝试再次注册相同用户名 → 应该看到错误提示 ❌

---

## 🎯 功能流程图

```
用户输入用户名
    ↓
前端验证格式
    ├─ 长度检查 (2-20字符)
    ├─ 字符检查 (中文/字母/数字/下划线)
    └─ 非空检查
    ↓
调用 Supabase 函数
    ↓
check_username_exists(username)
    ├─ 查询 auth.users 表
    └─ 比较 raw_user_meta_data->>'username'
    ↓
返回结果
    ├─ true  → ❌ 用户名已存在
    └─ false → ✅ 用户名可用
    ↓
注册成功或显示错误
```

---

## 🔍 如何查看已注册的用户名

### 方法 1：Supabase Dashboard

```
Dashboard → Authentication → Users
```

查看 `User Metadata` 列中的 `username` 字段

### 方法 2：SQL 查询

```sql
-- 查看所有用户名
SELECT 
  id,
  email,
  raw_user_meta_data->>'username' as username,
  created_at
FROM auth.users
ORDER BY created_at DESC;
```

---

## ⚠️ 重要说明

### 向下兼容

如果您**没有创建** `check_username_exists` 函数：

- ✅ 系统仍可正常运行
- ✅ 格式验证仍然有效（长度、字符限制）
- ⚠️ 但**不会检查用户名重复**
- ⚠️ 控制台会显示警告信息（不影响使用）

### 大小写处理

- 用户名 `TestUser` 和 `testuser` 被视为**相同**
- 存储时保留原始大小写
- 查询时不区分大小写

### 性能优化

对于大量用户的情况，建议创建索引：

```sql
-- 为用户元数据创建 GIN 索引（可选，提升查询性能）
CREATE INDEX IF NOT EXISTS idx_users_username 
ON auth.users 
USING gin (raw_user_meta_data);
```

---

## ✅ 功能清单总结

| 功能 | 邮箱 | 用户名 |
|------|------|--------|
| 唯一性检查 | ✅ 自动 | ✅ 需配置函数 |
| 格式验证 | ✅ 邮箱格式 | ✅ 2-20字符，中英数下划线 |
| 注册时验证 | ✅ | ✅ |
| 修改时验证 | ❌ 不可修改 | ✅ |
| 大小写敏感 | ✅ 区分 | ❌ 不区分 |

---

## 🧪 测试场景

### 测试 1：邮箱唯一性
```
✅ 注册：user1@test.com / pass123 / 用户A
❌ 注册：user1@test.com / pass456 / 用户B
   → 提示：用户已注册
```

### 测试 2：用户名唯一性
```
✅ 注册：user2@test.com / pass123 / 测试用户
❌ 注册：user3@test.com / pass456 / 测试用户
   → 提示：该用户名已被占用
```

### 测试 3：用户名格式
```
❌ 用户名：a
   → 提示：长度必须在 2-20 个字符之间
   
❌ 用户名：user@name
   → 提示：只能包含中文、字母、数字和下划线
   
✅ 用户名：用户_2024
   → 注册成功
```

### 测试 4：用户名大小写
```
✅ 注册：user4@test.com / pass123 / TestUser
❌ 注册：user5@test.com / pass456 / testuser
   → 提示：该用户名已被占用（不区分大小写）
```

---

## 📞 常见问题

### Q1: 如果忘记创建数据库函数会怎样？
**A:** 系统会继续运行，但不会检查用户名重复。建议尽快创建函数。

### Q2: 用户名可以包含空格吗？
**A:** 不可以。只允许中文、字母、数字和下划线。

### Q3: 用户名可以修改吗？
**A:** 可以！在个人主页点击用户名旁边的编辑按钮即可修改。

### Q4: 邮箱可以修改吗？
**A:** 目前不支持。如需修改，请注册新账号。

### Q5: 如何禁止某些敏感词作为用户名？
**A:** 可以在前端添加黑名单检查，或在数据库函数中添加过滤规则。

---

## 📝 开发者注意事项

### 前端文件位置
- 认证逻辑：`/src/contexts/auth-context.tsx`
- 登录/注册页面：`/src/app/components/auth-page.tsx`
- 个人主页：`/src/app/components/profile-page.tsx`

### 数据库函数位置
- SQL 文件：`/supabase_sql_username_check.sql`
- 执行位置：Supabase Dashboard → SQL Editor

### 测试建议
1. 创建测试账号进行各种场景测试
2. 检查控制台是否有错误信息
3. 验证所有错误提示是否正确显示

---

## 🎉 完成！

配置完成后，您的系统将具备完整的用户名和邮箱唯一性保护功能！
