# 🚀 快速修复："Email not confirmed" 错误

## ⚡ 3 分钟快速修复

### 步骤 1️⃣：登录 Supabase Dashboard
```
访问：https://supabase.com/dashboard/project/qbglfhdvkrhxspwrbsgq
```

### 步骤 2️⃣：禁用邮箱验证
```
左侧菜单 → Authentication → Providers → Email
找到 "Confirm email" 选项
设置为 OFF（关闭）
点击 Save 保存
```

### 步骤 3️⃣：确认现有用户（如果已注册过账号）
```
左侧菜单 → SQL Editor → New Query
粘贴以下 SQL 并点击 Run：
```

```sql
UPDATE auth.users 
SET email_confirmed_at = NOW(), confirmed_at = NOW()
WHERE email_confirmed_at IS NULL;
```

---

## ✅ 完成！

现在您可以：
- ✅ 注册新账号（无需邮箱验证）
- ✅ 登录已有账号
- ✅ 正常使用所有功能

---

## 📸 示例截图路径

**禁用邮箱验证的位置：**
```
Dashboard 
  → Authentication 
    → Providers 
      → Email 
        → Confirm email (关闭这个开关)
          → Save
```

**确认用户的位置（方法 2）：**
```
Dashboard 
  → Authentication 
    → Users 
      → 点击用户
        → Confirm Email 按钮
```

---

## 🔍 验证修复是否成功

### 测试 1：注册新用户
1. 访问网站
2. 点击"登录/注册"
3. 填写注册信息
4. 点击"注册"
5. ✅ 应该自动登录成功

### 测试 2：登录已有用户
1. 使用邮箱和密码登录
2. ✅ 应该成功登录

---

## 💡 如果还有问题

### 清除浏览器缓存
```
按 Ctrl + Shift + Delete (Windows)
按 Cmd + Shift + Delete (Mac)
清除缓存后刷新页面
```

### 检查 SQL 是否执行成功
```sql
-- 查看所有用户的确认状态
SELECT 
  email,
  email_confirmed_at,
  CASE 
    WHEN email_confirmed_at IS NOT NULL THEN '✅ 已确认'
    ELSE '❌ 未确认'
  END as status
FROM auth.users;
```

---

## 📞 需要帮助？

查看详细文档：`/supabase_fix_email_confirmation.md`

---

**最后更新：** 2026-01-30
